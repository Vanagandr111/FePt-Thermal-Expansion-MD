# Fe-Pt MD Thermal Expansion — Python dependencies
# Протестировано на Python 3.11 / 3.12
# Требует Python ≤ 3.12 (bootstrap.bat проверяет)
# Жёсткие версии — pre-built wheels, без сборки из исходников
numpy==1.26.4
matplotlib==3.8.4
